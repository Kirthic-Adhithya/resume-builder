"""Concrete repository implementations (SQLAlchemy-backed), one per aggregate.

Each class here implements a Protocol from `application.interfaces.repositories` — e.g.
`SqlAlchemyUserRepository` implements `UserRepository`. Populated starting in Phase 1 (Auth).
"""
